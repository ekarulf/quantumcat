version: 2
updates:
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      # The local replacement is a reduced source snapshot. Update its source
      # and module metadata together during deliberate upstream rebases.
      - dependency-name: "github.com/tailscale/tailcat"
